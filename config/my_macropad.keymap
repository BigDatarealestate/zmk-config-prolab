#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h> // 블루투스 기능을 위해 이 줄이 꼭 필요합니다.

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &bt BT_CLR_ALL      &kp F13      &kp F14
                &kp F15             &kp F16      &kp F17
            >;
        };
    };
};
